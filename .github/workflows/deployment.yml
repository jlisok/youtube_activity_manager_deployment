name: deployment
on:
  workflow_dispatch:
  
jobs:
  deploy:
    runs-on: ubuntu-latest
    env: 
      EC2_SECRET: ${{ secrets.EC2_KEY }}

    steps:
      - uses: actions/checkout@v2
      
      - name: run deployment
        run: |
          chmod +x ci/deploy.sh
          ./ci/deploy.sh
          

